Simple REST API where you can create differend animals.

## TO RUN PROJECT USE

1. `npm install`
2. `npm start`

Finally, navigate to `http://localhost:5000/home` and you should see the API running!

